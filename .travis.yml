language: julia
os:
  - linux
  - osx
julia:
  - 0.6
  - nightly
notifications:
  email: false
script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  # - julia -e 'Pkg.clone("https://github.com/INSIGNEO/openBF.git")'
  # - cd test/
  # - julia --code-coverage runtests.jl
  # - cd ../
  - julia -e 'Pkg.clone(pwd()); Pkg.build("openBF"); Pkg.test("openBF"; coverage=true)'
after_success:
  - echo "done!"
<<<<<<< HEAD
  - julia -e 'cd(Pkg.dir("openBF")); Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())'
=======
  # - julia -e 'Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())'
>>>>>>> master
matrix:
  fast_finish: true
  allow_failures:
    - julia: nightly
    - os: osx
